{
  "name": "game-asset-ranking",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-hardware-data.mjs && node scripts/build-market-data.mjs && node scripts/build-seo.mjs",
    "check": "node scripts/validate-site.mjs",
    "test": "npm run build && npm run check"
  }
}
